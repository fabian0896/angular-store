import { TotalValuePipe } from './total-value.pipe';

describe('TotalValuePipe', () => {
  it('create an instance', () => {
    const pipe = new TotalValuePipe();
    expect(pipe).toBeTruthy();
  });
});
